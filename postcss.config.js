export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // autoprefixer is usually still good to have, though v4 might bundle it. Safe to keep for now unless it errors.
    },
}
